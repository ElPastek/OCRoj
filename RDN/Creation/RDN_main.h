# include <stdio.h>
# include <stdlib.h>
# include "../network.h"

/*
void train(s_network *c, size_t nb_training_set, double *inputs);

int verif(double *tab, size_t len);

int verif_2(s_network *container, size_t wanted);

void save(s_network *container);


void open_training(size_t nb_ins, size_t nb_col, size_t nb_hne, size_t nb_out,
				   size_t nb_training_set, double *inputs);*/